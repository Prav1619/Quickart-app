name: Smart CI - Quickart

on:
  push:
    branches: [main]
    paths:
      - 'product-service/**'
      - 'user-service/**'
      - 'order-service/**'
      - 'pom.xml'
    paths-ignore:
      - '**/*.md'
      - '.github/workflows/**'

  workflow_dispatch:
jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      # ----------------------------------
      # 1. Checkout full repo history
      # ----------------------------------
      - name: Checkout code
        uses: actions/checkout@v4
        with:
          fetch-depth: 0   # REQUIRED for diff to work

      # ----------------------------------
      # 2. Setup Java
      # ----------------------------------
      - name: Setup Java
        uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: 17
          cache: maven
      - name: Cache Maven dependencies
        uses: actions/cache@v4
        with:
          path: ~/.m2/repository
          key: maven-${{ runner.os }}-${{ hashFiles('**/pom.xml') }}
          restore-keys: |
            maven-${{ runner.os }}-
      # ----------------------------------
      # 3. Detect changed files
      # ----------------------------------
      - name: Detect changes
        id: changes
        run: |
          BEFORE=${{ github.event.before }}
          AFTER=${{ github.sha }}

          echo "Before: $BEFORE"
          echo "After:  $AFTER"

          # Handle edge cases safely
          if git cat-file -e $BEFORE^{commit} 2>/dev/null; then
            FILES=$(git diff --name-only $BEFORE $AFTER)
          else
            echo "Invalid BEFORE commit → fallback"
            FILES=""
          fi

          echo "Changed files:"
          echo "$FILES"

          echo "FILES=$FILES" >> $GITHUB_ENV

      # ----------------------------------
      # 4. Determine impacted services
      # ----------------------------------
      - name: Determine impacted services
        run: |
          SERVICES=""

          # Global changes → build all
          if echo "$FILES" | grep -E 'pom.xml|k8s/|scripts/|terraform/'; then
            SERVICES="product-service user-service order-service"
          else
            echo "$FILES" | grep -q "product-service/" && SERVICES="$SERVICES product-service"
            echo "$FILES" | grep -q "user-service/" && SERVICES="$SERVICES user-service"
            echo "$FILES" | grep -q "order-service/" && SERVICES="$SERVICES order-service"
          fi

          # Fallback if nothing detected
          if [ -z "$SERVICES" ]; then
            echo "No service detected → fallback to full build"
            SERVICES="product-service user-service order-service"
          fi

          echo "Final impacted services: $SERVICES"
          echo "SERVICES=$SERVICES" >> $GITHUB_ENV

      # ----------------------------------
      # 5. Build (efficient way)
      # ----------------------------------
      - name: Build impacted services
        run: |
          echo "Building services: $SERVICES"
          mvn clean package -pl $(echo $SERVICES | tr ' ' ',') -am

      # ----------------------------------
      # 6. Verify outputs
      # ----------------------------------
      - name: List generated JARs
        run: find . -name "*.jar"

      # ----------------------------------
      # 7. Upload artifacts
      # ----------------------------------
      - name: Upload artifacts
        uses: actions/upload-artifact@v4
        with:
          name: microservices-artifacts
          path: "**/target/*.jar"
